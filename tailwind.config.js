const colors = require('tailwindcss/colors')

module.exports = {
  darkMode: 'class',
  purge: {
    enabled: false,
    content: ['./src/**/*.html', './src/**/*.vue', './src/**/*.svg']
  },
  theme: {},
  variants: {},
  plugins: []
}
